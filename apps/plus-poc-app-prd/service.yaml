apiVersion: v1
kind: Service
metadata:
  name: plus-poc-app-prd
spec:
  type: NodePort
  selector:
    app: plus-poc-app-prd
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30050
