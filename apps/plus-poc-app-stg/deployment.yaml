apiVersion: apps/v1
kind: Deployment
metadata:
  name: plus-poc-app-stg
  labels:
    app: plus-poc-app-stg
    env: stg
  annotations:
    # 플랫폼(S6)이 GitOps 푸시 시 갱신한다.
    jira.issue: TPS-6602
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plus-poc-app-stg
  template:
    metadata:
      labels:
        app: plus-poc-app-stg
        env: stg
      annotations:
        jira.issue: TPS-6602
    spec:
      containers:
        - name: app
          # 규칙 4 — tag 가 아니라 digest 로 고정한다.
          image: docker.io/scmkim/plus_poc_app@sha256:1090e79d77adf2db3afb826e2b2d08a5bf2e1872b859c789741f8995d5d282de
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: '8080'
            - name: APP_ENV
              value: STG
            - name: JIRA_ISSUE
              value: TPS-6602
            - name: APP_VERSION
              value: '0.2.0'
            - name: SOURCE_REPO
              value: devopsbmpart/plus_poc_app
            - name: IMAGE_DIGEST
              value: sha256:1090e79d77adf2db3afb826e2b2d08a5bf2e1872b859c789741f8995d5d282de
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
